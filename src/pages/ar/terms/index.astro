---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import TextPageTemplate from '../../../components/pages/TextPageTemplate.astro';
import { getPageContent } from '../../../lib/content';

const currentPath = Astro.url.pathname;
const pageContent = getPageContent('terms');
---

<BaseLayout lang="ar" title={`${pageContent.titleAr} — SZAFIT`} currentPath={currentPath} showNav>
  <TextPageTemplate lang="ar" page="terms" />
</BaseLayout>
