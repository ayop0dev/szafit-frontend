---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import CheckoutForm from '../../../components/checkout/CheckoutForm.astro';

const currentPath = Astro.url.pathname;
---
<BaseLayout lang="en" title="Complete Order — SZAFIT" currentPath={currentPath} showNav>
  <main class="flex-1 bg-surface-soft min-h-screen">
    <CheckoutForm lang="en" />
  </main>
</BaseLayout>
